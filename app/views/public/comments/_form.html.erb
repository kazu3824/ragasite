<%= form_with(model:[track, comment],url: public_track_comments_path(track.id), local: false) do |f| %>
  <%= f.text_area :body, rows:'5',placeholder: "コメントをここに", class: "w-100", id: "comment_text" %>
  <%= f.submit "送信する", class: "btn btn-success" %>
<% end %>